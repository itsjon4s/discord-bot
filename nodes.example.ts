import { NodeOptions } from 'vulkava';

export default {
  nodes: [
    {
      id: '01',
      hostname: 'localhost',
      port: 443,
      password: 'youshallnotpass'
    }
  ] as NodeOptions[]
};
